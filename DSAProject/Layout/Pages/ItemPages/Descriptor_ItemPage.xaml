<Page
    x:Class="DSAProject.Layout.Pages.Descriptor_ItemPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DSAProject.Layout.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <StackPanel Orientation="Horizontal" Spacing="5">
        <TextBlock x:Name="XAML_Title" Text="{x:Bind Descriptor.DescriptionTitle,  Mode=OneWay}" Width="{x:Bind WidthTitle, Mode=TwoWay}" Foreground="Black" TextAlignment="Left" VerticalAlignment="Center" HorizontalAlignment="Left" FontFamily="French Script MT" FontSize="{x:Bind OwnFontSize, Mode=TwoWay}" TextWrapping="Wrap" MaxLines="1" ></TextBlock>
        <TextBlock x:Name="XAML_Text"  Text="{x:Bind Descriptor.DescriptionText, Mode=OneWay}" Width="{x:Bind WIdthText, Mode=TwoWay}"  Foreground="Black" TextAlignment="Left" VerticalAlignment="Center" HorizontalAlignment="Left" FontFamily="French Script MT" FontSize="{x:Bind OwnFontSize, Mode=TwoWay}" TextWrapping="Wrap" MaxLines="1"></TextBlock>
    </StackPanel>
</Page>
