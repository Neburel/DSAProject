<Page
    x:Class="DSAProject.Layout.Pages.Descriptor_ItemPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DSAProject.Layout.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid ColumnSpacing="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" x:Name="XAML_Title" Text="{x:Bind Descriptor.DescriptionTitle,  Mode=OneWay}" Width="{x:Bind WidthTitle, Mode=TwoWay}"   Foreground="Black" TextAlignment="Left" VerticalAlignment="Center" HorizontalAlignment="Left" FontFamily="French Script MT" FontSize="{x:Bind OwnFontSize, Mode=TwoWay}" TextWrapping="Wrap"></TextBlock>
        <TextBlock Grid.Column="1" x:Name="XAML_Text"  Text="{x:Bind Descriptor.DescriptionText, Mode=OneWay}" Width="{x:Bind WIdthText, Mode=TwoWay}"      Foreground="Black" TextAlignment="Left" VerticalAlignment="Center" HorizontalAlignment="Left" FontFamily="French Script MT" FontSize="{x:Bind OwnFontSize, Mode=TwoWay}" TextWrapping="Wrap"></TextBlock>
    </Grid>
</Page>
