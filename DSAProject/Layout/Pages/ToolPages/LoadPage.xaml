<Page
    x:Class="DSAProject.Layout.Pages.LoadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:json="using:DSALib.Classes.JSON"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView
            x:Name="XAML_LoadListView"
            ItemsSource="{x:Bind Items}"
            ItemClick="XAML_LoadListView_ItemClick"
            IsItemClickEnabled="True">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="json:JSON_Charakter">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="{x:Bind Name}" FontFamily="French Script MT" FontSize="24" FontStyle="Oblique"/>
                        <TextBlock Text="{x:Bind SaveTimeAsString}" FontFamily="French Script MT" FontSize="24" FontStyle="Oblique"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            
        </ListView>
    </Grid>
</Page>
